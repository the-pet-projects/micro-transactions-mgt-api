version: '2'

services:
  microtransactionmgtapi:
    container_name: microtransactionmgtapi
    image: petprojects/micro-transactions-mgt-api:dev
    build:
      context: ./src/API
      dockerfile: Dockerfile
      args:
        source: ./build
    ports:
      - "5000:80"
